{% extends "base.html" %}
{% block title %}Лог операций{% endblock %}
{% block content %}
<h2>📄 Журнал операций</h2>
<table>
  <tr>
    <th>Дата</th><th>Сотрудник</th><th>Категория</th><th>Подкатегория</th><th>Операция</th><th>Кол-во</th>
  </tr>
  {% for op in operations %}
  <tr>
    <td>{{ op.date }}</td>
    <td>{{ op.user }}</td>
    <td>{{ op.category }}</td>
    <td>{{ op.subcategory }}</td>
    <td>{{ op.operation }}</td>
    <td>{{ op.count }}</td>
  </tr>
  {% endfor %}
</table>
<a href="/dashboard">⬅️ Назад</a>
{% endblock %}
