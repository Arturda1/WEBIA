{% extends "base.html" %}
{% block title %}Расчёт себестоимости{% endblock %}
{% block content %}
<h2>📦 Расчёт себестоимости (только готовые изделия)</h2>
<form method="post">
  <label for="product">Выберите изделие:</label>
  <select id="product" name="product" required>
    {% for p in products %}
    <option value="{{ p }}">{{ p }}</option>
    {% endfor %}
  </select>
  <button type="submit">📊 Посчитать</button>
</form>
<a href="/dashboard">⬅️ Назад</a>
{% endblock %}
